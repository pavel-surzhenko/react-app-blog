// components
import { Posts, RecentComments } from '../../components';

export const Feed = () => {
    return (
        <>
            <Posts />
            <RecentComments />
        </>
    );
};
